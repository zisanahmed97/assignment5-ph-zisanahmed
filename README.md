1.
var: It is the old way to declare a variable in JavaScript.
let: It can be updated but cannot be re-declared in the same scope. 
const: It is used for variables whose value should not change.

2.
The spread operator ... is used to expand or copy elements of an array or object.
3.map(): Creates a new array by modifying each element.
filter(): Creates a new array with elements that match a condition.
forEach(): Loops through an array but does not return a new arr
4.An arrow function is a shorter and cleaner way to write a function in JavaScript. it is a version of es6
5.Template literalsis used to write strings and easily include variables using backticks ( ).
